// Global setup for Playwright tests
// This would typically start the dev server, but we'll keep it simple for now

export default async function globalSetup() {
  console.log('Global setup for tests');
  // In a real implementation, you would start your dev server here
  // and clean up after tests are complete
}